# Use a lightweight base image
FROM alpine:latest

# Add a custom message or script to be executed
CMD echo "Hello from Your Custom Docker Image! This message shows that your custom Docker image is working correctly."
